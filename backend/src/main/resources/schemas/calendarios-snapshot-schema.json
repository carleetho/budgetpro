{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Calendarios Snapshot Schema",
  "description": "Esquema para validar la estructura de calendarios y restricciones temporales del cronograma en snapshots",
  "type": "object",
  "required": ["calendarios", "diasFestivos", "restricciones"],
  "properties": {
    "calendarios": {
      "type": "array",
      "description": "Lista de calendarios de trabajo (inicialmente vacío, preparado para futuras extensiones)",
      "items": {
        "type": "object"
      }
    },
    "diasFestivos": {
      "type": "array",
      "description": "Lista de días festivos (inicialmente vacío, preparado para futuras extensiones)",
      "items": {
        "type": "string",
        "format": "date"
      }
    },
    "restricciones": {
      "type": "array",
      "description": "Lista de restricciones temporales (inicialmente vacío, preparado para futuras extensiones)",
      "items": {
        "type": "object"
      }
    }
  },
  "additionalProperties": false
}
